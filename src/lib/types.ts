export type FooterNavLink = {
  link: string;
  name: string;
};

export type DropdownItem = {
  title: string;
  href: string;
  description: string;
};

export type Users = {
  id: number;
  name: string;
  date: string;
};
